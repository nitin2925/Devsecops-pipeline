name: sonar scan

on:
 workflow_call:

jobs:
  sonar:
   runs-on: self-hosted

   steps:
    - name: SonarQubeScan
      run: mvn -B verify sonar:sonar -Dsonar.projectKey=nitin2925_Devsecops-pipeline -Dsonar.organization=nitin2925 -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_TOKEN
      env: 
       SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    
    
