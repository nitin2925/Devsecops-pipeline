name: Test

on:
 workflow_call:

jobs:
 test:
  runs-on: self-hosted

  steps:
   - name: run unit tests
     run: mvn test
   
   - name: upload test results
     uses: actions/upload-artifact@v3
     with: 
      name: test results
      path: target/test-classes/TEST-*.xml
      
